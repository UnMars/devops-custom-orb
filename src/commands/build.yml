description: "Build the static website"
executor: node
steps:
  - checkout
  - run:
      name: Build Static Site
      command: |
        echo "Building static site..."
        npm install
        npm run build
